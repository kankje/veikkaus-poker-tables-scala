# veikkaus-poker-tables

## Requirements

- Docker

## Getting started

- `cp twitter4j.properties.dist twitter4j.properties` and fill in your Twitter API key
- Start the development environment with `docker-compose up`
